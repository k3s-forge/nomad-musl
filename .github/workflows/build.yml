name: Build Nomad for Alpine (musl)

"on":
  workflow_dispatch:
    inputs:
      version:
        description: 'Nomad version tag'
        required: false
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building latest or specified version"
